Function Test-Configuration 
{    
    [CmdletBinding()]
    param (
        [Parameter(Mandatory)]
        [string[]]$parameters
    )

    # TODO
    # this function should loop through the variables and perform validation on them to make sure they are valid before proceeding with the build

}